diff --git a/dist/cjs/ReactEditorView.js b/dist/cjs/ReactEditorView.js
index aedf7586a0b4922f5044909588fa9cd3b16e7e4a..fd5b3cf7f7684c836c97289457519b22e7fd77a6 100644
--- a/dist/cjs/ReactEditorView.js
+++ b/dist/cjs/ReactEditorView.js
@@ -203,7 +203,11 @@ let ReactEditorView = class ReactEditorView extends _prosemirrorview.EditorView
             this.domObserver.setCurSelection();
             this.input.mouseDown = {
                 allowDefault: false,
-                delayedSelectionSync: false
+                delayedSelectionSync: false,
+                // prosemirror-view 1.41.9+ calls delaySelUpdate()/done() on
+                // input.mouseDown; this stub object must provide them.
+                delaySelUpdate: ()=>true,
+                done: ()=>{}
             };
         }
         super.update(this.nextProps);
diff --git a/dist/cjs/plugins/beforeInputPlugin.js b/dist/cjs/plugins/beforeInputPlugin.js
index f44e77316a4207d8a83139f1cf82346fc1c22a5a..070051d707a78cf692867f3157539d557ffc772d 100644
--- a/dist/cjs/plugins/beforeInputPlugin.js
+++ b/dist/cjs/plugins/beforeInputPlugin.js
@@ -40,11 +40,14 @@ function insertText(view, eventData) {
 // it happens too late. We snapshot the DOM during compositionstart, but
 // the gapcursor hack runs in beforeinput (after compositionstart).
 function handleGapCursorComposition(view) {
-    // @ts-expect-error Internal property - jsonID
-    if (!(view.state.selection.jsonID === "gapcursor")) {
+    // Create an inline context whenever the composition starts outside a
+    // textblock — a gap cursor between blocks, or the position left after a
+    // top-level node selection was deleted. Without a textblock the IME has
+    // nowhere to compose and $from.before() (used below) would throw.
+    const { $from } = view.state.selection;
+    if ($from.parent.isTextblock) {
         return;
     }
-    const { $from } = view.state.selection;
     const insert = $from.parent.contentMatchAt($from.index())// All schemas _must_ have a text node type
     // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
     .findWrapping(view.state.schema.nodes.text);
@@ -150,7 +153,18 @@ function beforeInputPlugin() {
                             view.domObserver.connectSelection();
                         }
                     }
-                    const freezeFrom = view.state.selection.$from.before();
+                    const _$from = view.state.selection.$from;
+                    if (_$from.depth === 0) {
+                        // No enclosing block to freeze (e.g. a gap cursor between
+                        // top-level nodes that could not be wrapped). Bail out and
+                        // let prosemirror-view handle the composition natively.
+                        view.dispatch(view.state.tr.setMeta(reactKeysPluginKey, {
+                            cursorWrapper: null,
+                            freezeFrom: null
+                        }));
+                        return false;
+                    }
+                    const freezeFrom = _$from.before();
                     view.dispatch(view.state.tr.setMeta(_reactKeys.reactKeysPluginKey, {
                         freezeFrom
                     }));
@@ -182,6 +196,20 @@ function beforeInputPlugin() {
                     if (!(view instanceof _ReactEditorView.ReactEditorView)) return false;
                     if (!view.composing) return false;
                     teardownCompositionAndUnfreeze(view, event);
+                    // Restore the DOM caret to PM's selection. Committing a
+                    // composition replaces the composed text node and re-renders,
+                    // collapsing the browser selection to the start of the block.
+                    // PM's selection is already correct, but commitPendingEffects
+                    // does not always re-sync the DOM caret here, so the visible
+                    // caret would otherwise stay at line start.
+                    const { selection } = view.state;
+                    view.domObserver.disconnectSelection();
+                    try {
+                        view.docView.setSelection(selection.anchor, selection.head, view, true);
+                    } finally{
+                        view.domObserver.setCurSelection();
+                        view.domObserver.connectSelection();
+                    }
                     return true;
                 },
                 contextmenu (view, event) {
@@ -299,6 +327,11 @@ function beforeInputPlugin() {
     });
 }
 function syncCompositionViewDescs(view) {
+    // The MutationObserver that calls this can fire while ReactEditorView is
+    // between renders, when docView is transiently null (it is nulled in the
+    // constructor and reassigned by a React layout effect). Bail rather than
+    // dereference it.
+    if (!view.docView) return;
     const compositionNode = view.domObserver.lastChangedTextNode;
     if (!compositionNode) return;
     const freezeFrom = _reactKeys.reactKeysPluginKey.getState(view.state)?.freezeFrom;
diff --git a/dist/cjs/viewdesc.js b/dist/cjs/viewdesc.js
index 0f027afc25c5132b30f29d4f1e28adf1d1f38798..81c849e68b4104b13119f9ea91c113e64c3e1467 100644
--- a/dist/cjs/viewdesc.js
+++ b/dist/cjs/viewdesc.js
@@ -697,9 +697,15 @@ let TextViewDesc = class TextViewDesc extends NodeViewDesc {
         return false;
     }
     domFromPos(pos) {
+        // Clamp to the actual DOM text length. nodeDOM can transiently lag the
+        // model (e.g. after an IME composition repoints it at a shorter
+        // composition text node), and an offset past the DOM node length makes
+        // Range.setEnd throw in coordsAtPos/scrollToSelection. Mirrors the
+        // Math.min clamp already used in localPosFromDOM below.
+        const max = this.nodeDOM && this.nodeDOM.nodeValue != null ? this.nodeDOM.nodeValue.length : pos;
         return {
             node: this.nodeDOM,
-            offset: pos
+            offset: Math.min(pos, max)
         };
     }
     localPosFromDOM(dom, offset, bias) {
diff --git a/dist/esm/ReactEditorView.js b/dist/esm/ReactEditorView.js
index 3be005610d611419423a399f1be72ab2cde310ea..90a1709b6ac078164bfb87f10bf1afcd610d9a0a 100644
--- a/dist/esm/ReactEditorView.js
+++ b/dist/esm/ReactEditorView.js
@@ -203,7 +203,11 @@ function changedNodeViews(a, b) {
             this.domObserver.setCurSelection();
             this.input.mouseDown = {
                 allowDefault: false,
-                delayedSelectionSync: false
+                delayedSelectionSync: false,
+                // prosemirror-view 1.41.9+ calls delaySelUpdate()/done() on
+                // input.mouseDown; this stub object must provide them.
+                delaySelUpdate: ()=>true,
+                done: ()=>{}
             };
         }
         super.update(this.nextProps);
diff --git a/dist/esm/plugins/beforeInputPlugin.js b/dist/esm/plugins/beforeInputPlugin.js
index e9ff6d74e963488642f855d95a9f513ef32393ae..44f994f408978909ef4002941bc59bbaccbfbd85 100644
--- a/dist/esm/plugins/beforeInputPlugin.js
+++ b/dist/esm/plugins/beforeInputPlugin.js
@@ -30,11 +30,14 @@ function insertText(view, eventData) {
 // it happens too late. We snapshot the DOM during compositionstart, but
 // the gapcursor hack runs in beforeinput (after compositionstart).
 function handleGapCursorComposition(view) {
-    // @ts-expect-error Internal property - jsonID
-    if (!(view.state.selection.jsonID === "gapcursor")) {
+    // Create an inline context whenever the composition starts outside a
+    // textblock — a gap cursor between blocks, or the position left after a
+    // top-level node selection was deleted. Without a textblock the IME has
+    // nowhere to compose and $from.before() (used below) would throw.
+    const { $from } = view.state.selection;
+    if ($from.parent.isTextblock) {
         return;
     }
-    const { $from } = view.state.selection;
     const insert = $from.parent.contentMatchAt($from.index())// All schemas _must_ have a text node type
     // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
     .findWrapping(view.state.schema.nodes.text);
@@ -140,7 +143,18 @@ export function beforeInputPlugin() {
                             view.domObserver.connectSelection();
                         }
                     }
-                    const freezeFrom = view.state.selection.$from.before();
+                    const _$from = view.state.selection.$from;
+                    if (_$from.depth === 0) {
+                        // No enclosing block to freeze (e.g. a gap cursor between
+                        // top-level nodes that could not be wrapped). Bail out and
+                        // let prosemirror-view handle the composition natively.
+                        view.dispatch(view.state.tr.setMeta(reactKeysPluginKey, {
+                            cursorWrapper: null,
+                            freezeFrom: null
+                        }));
+                        return false;
+                    }
+                    const freezeFrom = _$from.before();
                     view.dispatch(view.state.tr.setMeta(reactKeysPluginKey, {
                         freezeFrom
                     }));
@@ -172,6 +186,20 @@ export function beforeInputPlugin() {
                     if (!(view instanceof ReactEditorView)) return false;
                     if (!view.composing) return false;
                     teardownCompositionAndUnfreeze(view, event);
+                    // Restore the DOM caret to PM's selection. Committing a
+                    // composition replaces the composed text node and re-renders,
+                    // collapsing the browser selection to the start of the block.
+                    // PM's selection is already correct, but commitPendingEffects
+                    // does not always re-sync the DOM caret here, so the visible
+                    // caret would otherwise stay at line start.
+                    const { selection } = view.state;
+                    view.domObserver.disconnectSelection();
+                    try {
+                        view.docView.setSelection(selection.anchor, selection.head, view, true);
+                    } finally{
+                        view.domObserver.setCurSelection();
+                        view.domObserver.connectSelection();
+                    }
                     return true;
                 },
                 contextmenu (view, event) {
@@ -289,6 +317,11 @@ export function beforeInputPlugin() {
     });
 }
 function syncCompositionViewDescs(view) {
+    // The MutationObserver that calls this can fire while ReactEditorView is
+    // between renders, when docView is transiently null (it is nulled in the
+    // constructor and reassigned by a React layout effect). Bail rather than
+    // dereference it.
+    if (!view.docView) return;
     const compositionNode = view.domObserver.lastChangedTextNode;
     if (!compositionNode) return;
     const freezeFrom = reactKeysPluginKey.getState(view.state)?.freezeFrom;
diff --git a/dist/esm/viewdesc.js b/dist/esm/viewdesc.js
index bb30fe2286369f6767b04588a815a15f80812fa0..b8ed6bbd9ebcceab1e18ed5dae2ad1adf6effdb7 100644
--- a/dist/esm/viewdesc.js
+++ b/dist/esm/viewdesc.js
@@ -673,9 +673,15 @@ export class TextViewDesc extends NodeViewDesc {
         return false;
     }
     domFromPos(pos) {
+        // Clamp to the actual DOM text length. nodeDOM can transiently lag the
+        // model (e.g. after an IME composition repoints it at a shorter
+        // composition text node), and an offset past the DOM node length makes
+        // Range.setEnd throw in coordsAtPos/scrollToSelection. Mirrors the
+        // Math.min clamp already used in localPosFromDOM below.
+        const max = this.nodeDOM && this.nodeDOM.nodeValue != null ? this.nodeDOM.nodeValue.length : pos;
         return {
             node: this.nodeDOM,
-            offset: pos
+            offset: Math.min(pos, max)
         };
     }
     localPosFromDOM(dom, offset, bias) {
diff --git a/src/tiptap/README.md b/src/tiptap/README.md
deleted file mode 100644
index 72f4b2501664b3eda609bc6bad8eb14dac2995f2..0000000000000000000000000000000000000000
