diff --git a/dist/cjs/plugins/beforeInputPlugin.js b/dist/cjs/plugins/beforeInputPlugin.js
index e947094de36bfc6d8bd083d299bb0dea29231581..7879c24fc8cd4b1cb8e11066cb4a8744d2f74c31 100644
--- a/dist/cjs/plugins/beforeInputPlugin.js
+++ b/dist/cjs/plugins/beforeInputPlugin.js
@@ -9,6 +9,7 @@ Object.defineProperty(exports, "beforeInputPlugin", {
     }
 });
 const _prosemirrorstate = require("prosemirror-state");
+const _prosemirrormodel = require("prosemirror-model");
 const _CursorWrapper = require("../components/CursorWrapper.js");
 const _ReactWidgetType = require("../decorations/ReactWidgetType.js");
 function insertText(view, eventData) {
@@ -34,23 +35,39 @@ function beforeInputPlugin(setCursorWrapper) {
                 compositionstart (view) {
                     const { state } = view;
                     view.dispatch(state.tr.deleteSelection());
-                    const $pos = state.selection.$from;
-                    compositionMarks = state.storedMarks ?? $pos.marks();
+                    let currentState = view.state;
+                    let $pos = currentState.selection.$from;
+                    if (!$pos.parent.isTextblock) {
+                        const wrap = $pos.parent
+                            .contentMatchAt($pos.index())
+                            .findWrapping(currentState.schema.nodes.text);
+                        if (!wrap) return false;
+                        let frag = _prosemirrormodel.Fragment.empty;
+                        for (let i = wrap.length - 1; i >= 0; i--) {
+                            frag = _prosemirrormodel.Fragment.from(wrap[i].createAndFill(null, frag));
+                        }
+                        const tr = currentState.tr
+                            .replace($pos.pos, $pos.pos, new _prosemirrormodel.Slice(frag, 0, 0));
+                        tr.setSelection(
+                            _prosemirrorstate.TextSelection.near(tr.doc.resolve($pos.pos + 1))
+                        );
+                        view.dispatch(tr);
+                        currentState = view.state;
+                        $pos = currentState.selection.$from;
+                        if (!$pos.parent.isTextblock) return false;
+                    }
+                    compositionMarks = currentState.storedMarks ?? $pos.marks();
                     if (compositionMarks) {
-                        setCursorWrapper((0, _ReactWidgetType.widget)(state.selection.from, _CursorWrapper.CursorWrapper, {
+                        setCursorWrapper((0, _ReactWidgetType.widget)(currentState.selection.from, _CursorWrapper.CursorWrapper, {
                             key: "cursor-wrapper",
                             marks: compositionMarks
                         }));
                     }
-                    // Snapshot the siblings of the node that contains the
-                    // current cursor. We'll restore this later, so that React
-                    // doesn't panic about unknown DOM nodes.
                     const { node: parent } = view.domAtPos($pos.pos);
                     precompositionSnapshot = [];
                     for (const node of parent.childNodes){
                         precompositionSnapshot.push(node);
                     }
-                    // @ts-expect-error Internal property - input
                     view.input.composing = true;
                     return true;
                 },
@@ -58,26 +75,25 @@ function beforeInputPlugin(setCursorWrapper) {
                     return true;
                 },
                 compositionend (view, event) {
-                    // @ts-expect-error Internal property - input
                     view.input.composing = false;
                     const { state } = view;
                     const { node: parent } = view.domAtPos(state.selection.from);
                     if (precompositionSnapshot) {
-                        // Restore the snapshot of the parent node's children
-                        // from before the composition started. This gives us a
-                        // clean slate from which to dispatch our transaction
-                        // and trigger a React update.
-                        precompositionSnapshot.forEach((prevNode, i)=>{
-                            if (parent.childNodes.length <= i) {
-                                parent.appendChild(prevNode);
-                                return;
-                            }
-                            parent.replaceChild(prevNode, parent.childNodes.item(i));
-                        });
-                        if (parent.childNodes.length > precompositionSnapshot.length) {
-                            for(let i = precompositionSnapshot.length; i < parent.childNodes.length; i++){
-                                parent.removeChild(parent.childNodes.item(i));
+                        try {
+                            precompositionSnapshot.forEach((prevNode, i)=>{
+                                if (parent.childNodes.length <= i) {
+                                    parent.appendChild(prevNode);
+                                    return;
+                                }
+                                parent.replaceChild(prevNode, parent.childNodes.item(i));
+                            });
+                            if (parent.childNodes.length > precompositionSnapshot.length) {
+                                for(let i = precompositionSnapshot.length; i < parent.childNodes.length; i++){
+                                    parent.removeChild(parent.childNodes.item(i));
+                                }
                             }
+                        } catch (_e) {
+                            // DOM restoration failed — skip and let React reconcile.
                         }
                     }
                     if (event.data) {
diff --git a/dist/esm/plugins/beforeInputPlugin.js b/dist/esm/plugins/beforeInputPlugin.js
index 2af61e73f147284d5a894587df4de4bafc23aed0..5b8a51eceaa7de2a2f894039fd7b32b773e367e8 100644
--- a/dist/esm/plugins/beforeInputPlugin.js
+++ b/dist/esm/plugins/beforeInputPlugin.js
@@ -1,4 +1,5 @@
-import { Plugin } from "prosemirror-state";
+import { Plugin, TextSelection } from "prosemirror-state";
+import { Fragment, Slice } from "prosemirror-model";
 import { CursorWrapper } from "../components/CursorWrapper.js";
 import { widget } from "../decorations/ReactWidgetType.js";
 function insertText(view, eventData) {
@@ -24,10 +25,40 @@ export function beforeInputPlugin(setCursorWrapper) {
                 compositionstart (view) {
                     const { state } = view;
                     view.dispatch(state.tr.deleteSelection());
-                    const $pos = state.selection.$from;
-                    compositionMarks = state.storedMarks ?? $pos.marks();
+                    // Use the updated state after deletion — the old `state`
+                    // reference is stale once dispatch() has been called.
+                    let currentState = view.state;
+                    let $pos = currentState.selection.$from;
+                    // If the selection is not inside a textblock (e.g. a
+                    // GapCursor between block nodes), create a wrapping
+                    // textblock so the browser's IME has a text node to
+                    // compose into.  We must create it here (not in a
+                    // downstream plugin) so that we can take a DOM snapshot
+                    // immediately after — without a snapshot the
+                    // compositionend handler cannot restore the DOM before
+                    // calling insertText, which leads to doubled text.
+                    if (!$pos.parent.isTextblock) {
+                        const wrap = $pos.parent
+                            .contentMatchAt($pos.index())
+                            .findWrapping(currentState.schema.nodes.text);
+                        if (!wrap) return false;
+                        let frag = Fragment.empty;
+                        for (let i = wrap.length - 1; i >= 0; i--) {
+                            frag = Fragment.from(wrap[i].createAndFill(null, frag));
+                        }
+                        const tr = currentState.tr
+                            .replace($pos.pos, $pos.pos, new Slice(frag, 0, 0));
+                        tr.setSelection(
+                            TextSelection.near(tr.doc.resolve($pos.pos + 1))
+                        );
+                        view.dispatch(tr);
+                        currentState = view.state;
+                        $pos = currentState.selection.$from;
+                        if (!$pos.parent.isTextblock) return false;
+                    }
+                    compositionMarks = currentState.storedMarks ?? $pos.marks();
                     if (compositionMarks) {
-                        setCursorWrapper(widget(state.selection.from, CursorWrapper, {
+                        setCursorWrapper(widget(currentState.selection.from, CursorWrapper, {
                             key: "cursor-wrapper",
                             marks: compositionMarks
                         }));
@@ -57,17 +88,31 @@ export function beforeInputPlugin(setCursorWrapper) {
                         // from before the composition started. This gives us a
                         // clean slate from which to dispatch our transaction
                         // and trigger a React update.
-                        precompositionSnapshot.forEach((prevNode, i)=>{
-                            if (parent.childNodes.length <= i) {
-                                parent.appendChild(prevNode);
-                                return;
-                            }
-                            parent.replaceChild(prevNode, parent.childNodes.item(i));
-                        });
-                        if (parent.childNodes.length > precompositionSnapshot.length) {
-                            for(let i = precompositionSnapshot.length; i < parent.childNodes.length; i++){
-                                parent.removeChild(parent.childNodes.item(i));
+                        //
+                        // The restoration is wrapped in a try/catch because
+                        // structural edits dispatched during composition (e.g.
+                        // by gap-cursor or node-selection plugins) may move or
+                        // remove the snapshotted nodes, making the restore
+                        // impossible.  In that case we skip restoration and
+                        // let React reconcile the DOM on the next render.
+                        try {
+                            precompositionSnapshot.forEach((prevNode, i)=>{
+                                if (parent.childNodes.length <= i) {
+                                    parent.appendChild(prevNode);
+                                    return;
+                                }
+                                parent.replaceChild(prevNode, parent.childNodes.item(i));
+                            });
+                            if (parent.childNodes.length > precompositionSnapshot.length) {
+                                for(let i = precompositionSnapshot.length; i < parent.childNodes.length; i++){
+                                    parent.removeChild(parent.childNodes.item(i));
+                                }
                             }
+                        } catch (_e) {
+                            // DOM restoration failed — structural changes during
+                            // composition made the snapshot stale.  React will
+                            // reconcile the DOM when the next transaction is
+                            // flushed below.
                         }
                     }
                     if (event.data) {
